{
    ?K math:notEqualTo ?N.
    $?K + 1$ = ?K1.
    $?K * 2$ = ?K2.
    $?P0 + (?S / (?K2 * (?K2 + 1) * (?K2 + 2)))$ = ?P1.
    (?K1 ?N ?P1 ?P $-?S$) :pi true.
} => {
    (?K ?N ?P0 ?P ?S) :pi true
}